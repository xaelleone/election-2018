import React, { Component } from 'react';

class ZipForm extends Component {
  // props: onSelectZip (callback, takes zip)

  render() {
    return (
      <div>YO I'M A ZIPFORM</div>
    );
    // display input text boxes and possibly ask for address
  }
}

export default ZipForm;
